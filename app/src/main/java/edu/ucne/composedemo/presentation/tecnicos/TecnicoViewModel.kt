package edu.ucne.composedemo.presentation.tecnicos


import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import edu.ucne.composedemo.data.local.Dao.TecnicoDao
import kotlinx.coroutines.launch


/*
class TecnicoViewModel(private val tecnicoDao: TecnicoDao) : ViewModel() {

    // Función para eliminar un técnico
    fun eliminarTecnico(id: Int) {
        viewModelScope.launch {
            tecnicoDao.deleteById(id) // Llamada a DAO para eliminar el técnico
        }
    }
}*/

















